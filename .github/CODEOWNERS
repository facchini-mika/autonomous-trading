# CODEOWNERS — informational only.
#
# This is a single-operator project. GitHub forbids self-approval, so
# branch protection runs with `required_approving_review_count: 0` and
# `require_code_owner_reviews` has no merge-blocking effect. The file
# stays as a stewardship record; risk-sensitivity is enforced via the
# audit-log self-review pattern (specs/engineering.md §3, §4, §8 + CLAUDE.md
# "Reviewer rule").

* @facchini-mika

# Risk-sensitive code (specs/engineering.md §3) — every change here requires an
# AUDIT_LOG.md entry documenting the operator's safety review.
/risk/ @facchini-mika

# MAX_CAPITAL_EUR carrier (lands in Phase 3) — same audit-log requirement.
/shared/config/settings.py @facchini-mika
